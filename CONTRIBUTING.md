Please create an issue or comment on an existing issue before sending a pull request.

# Local setup

Run `make init` to apply file modification times to files in `internal/testdata/local` to make tests pass.
